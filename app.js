const numero = 5

for (let i = 1; i <=10; i++){
    console.log(` ${ numero } x ${ i } = ${ numero * i}`)
}

// .  module es un objeto global que existe en node para poder utilizar ese archivo en cualquier parte del codigo..

//. para importar ese archivo aplicamos destructuring y seria asi en el archivo principal, cons {} requiere('ruta de la carpeta que queremos exportar..')
